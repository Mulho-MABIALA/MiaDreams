/**
 * routes/fcm.js — DÉPRÉCIÉ
 * Remplacé par ./push.js (Web Push natif / VAPID, sans Firebase).
 * Ce fichier n'est plus monté dans server.js (voir '/api/push').
 * À supprimer manuellement.
 */
module.exports = require('./push');
