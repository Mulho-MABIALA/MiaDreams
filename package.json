{
  "name": "miadreams",
  "version": "1.0.0",
  "description": "MIA DREAMS & CO — Application complète (backend + frontend)",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "build": "cd frontend && npm install && npm run build",
    "install-backend": "cd backend && npm install --omit=dev",
    "test": "npm run test:backend && npm run test:frontend",
    "test:backend": "cd backend && node --test src/tests/**/*.test.js",
    "test:frontend": "cd frontend && npx vitest run",
    "test:coverage": "npm run test:coverage:backend && npm run test:coverage:frontend",
    "test:coverage:backend": "cd backend && node --test --experimental-test-coverage src/tests/**/*.test.js",
    "test:coverage:frontend": "cd frontend && npx vitest run --coverage"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
